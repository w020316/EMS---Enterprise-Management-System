<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ems.mapper.DepartmentMapper">
    <select id="findAll" resultType="Department">
        SELECT * FROM department ORDER BY id
    </select>
    <select id="findById" resultType="Department">
        SELECT * FROM department WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO department (name, description, manager)
        VALUES (#{name}, #{description}, #{manager})
    </insert>
    <update id="update">
        UPDATE department
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="manager != null">manager = #{manager},</if>
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM department WHERE id = #{id}
    </delete>
    <select id="countByManager" resultType="int">
        SELECT COUNT(*) FROM department WHERE manager = #{manager}
    </select>
    <select id="findByName" resultType="Department">
        SELECT * FROM department WHERE name = #{name}
    </select>
</mapper>
